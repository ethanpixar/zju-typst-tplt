% 直方图数据
x = 0:7;
y = [638, 1098, 0, 1860, 2481, 0, 2941, 982];

% 创建条形图
bar(x, y, 'FaceColor', '#0072BD'); % 使用蓝色，颜色与图片一致

% 添加数据标签到每个柱状图上方
text(x, y, string(y), 'vert', 'bottom', 'horiz', 'center');

% 设置标题和轴标签 (虽然图片中没有明确的轴标签，但我们添加标题)
title('Histogram');
% xlabel('Bins'); % 可以选择性添加 x 轴标签
% ylabel('Frequency'); % 可以选择性添加 y 轴标签

% 设置 y 轴的范围，使其与图片大致一致
ylim([0, 3200]); % 稍微超过最大值，留出空间显示标签

% 设置 x 轴刻度，使其与柱状图对齐
xticks(x);

% 移除图例，如果不需要的话
legend off;

% 显示网格线，如果需要的话 (图片中没有网格线)
grid off;

% 调整图像大小以更接近原始图像的比例 (可选)
% set(gcf, 'Position', [100, 100, 800, 600]);
